# Marketing AI Studio — runtime dependencies
# Cleaned from the original pip-freeze dump (which included torch, jupyter, streamlit, etc.)
# Phase 1: minimum needed for the server to start. Phase 2 swap will trim google-* libs.

# Web framework
fastapi==0.128.4
uvicorn[standard]==0.40.0
python-multipart==0.0.12
starlette==0.52.1

# Settings & validation
pydantic==2.12.5
pydantic-settings==2.12.0
python-dotenv==1.0.1
email-validator==2.2.0

# Database
motor==3.7.1
pymongo==4.10.1

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.3.0
PyJWT==2.11.0

# HTTP / async
httpx==0.28.1
requests==2.32.5
aiohttp==3.10.10
aiofiles==24.1.0

# Config / data
PyYAML==6.0.2
orjson==3.10.7

# Providers — current (will be replaced in Phase 2)
google-adk==1.24.1
google-genai==1.62.0
google-cloud-storage==2.19.0
google-cloud-aiplatform==1.136.0

# Providers — new (Phase 2 swap targets)
anthropic==0.101.0
boto3==1.35.24

# Web scraping
beautifulsoup4==4.12.3
lxml==5.3.0

# Media
Pillow==10.4.0
PyMuPDF==1.27.1
